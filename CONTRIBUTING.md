# Reflow 协作约定

两人团队、权限平等、轻流程。只有两条硬规则，其余靠判断。

## 硬规则

1. **main 受保护**：禁止直接 push，所有改动走 PR；PR 必须 CI 绿才能合。两人都遵守，没有例外。
2. **在自己的分支上工作**：`feat/xxx`、`fix/xxx` 等任意前缀。互推分支随便推，改别人分支前打声招呼即可。

## PR 流程

- 谁的 PR 谁合并——合并即作者确认所有问题已处理。
- **不要求互相 approve**：两人团队，approve 是负担。但合并前请读一遍 diff，CI 抓不到逻辑和设计问题。
- 合并策略默认 merge commit，保留历史；小修可用 squash。

## 帮别人修代码

按"问题是否只有一种正确改法"分流：

- **明确 bug / 笔误 / 缺测试**：直接推他的分支，在 PR 上留条评论说明改了什么。
- **设计或方向问题**：只留 review comment 列出问题和选项，让对方自己决定，不代改。
- **改动较多或涉及核心设计**：从他的分支切 `fixup/xxx` 分支，给他开 PR（base 选他的分支），让他 review 你的修法。

## 什么时候必须升级流程

轻流程扛不住、需要临时加码的场景：

- **破坏性改动**（删数据、改持久化格式、动核心类型）：即使小也走 PR，让对方过一眼。
- **两人同时动同一块代码**：先协调谁先谁后，别硬 merge。
- **CI 红了**：别往 main 合新东西，先修红。

## 分支清理

已合并的远程分支由合并者负责删除。本地分支各自清理，别积压。
